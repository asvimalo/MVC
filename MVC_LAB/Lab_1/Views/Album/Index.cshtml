@model IEnumerable<Lab_1.Models.AlbumViewModel>

@{
    ViewBag.Title = "Index";
}

@{
    var options = new AjaxOptions
    {
        UpdateTargetId = "newAlbum",
        InsertionMode = InsertionMode.Replace,
        HttpMethod = "GET"
    };
}

<h2>Index</h2>

<div class="container">
    <div class="col-sm-12">
        <h3 class="caption">@Ajax.ActionLink("New Album", "Create", null, options, htmlAttributes: new { @class = "btn btn-success" })</h3>
        <div id="newAlbum"></div>
        <div class="loader" style="display: none"></div>
    </div>
    <div class="row" id="albumList">
        @Html.Partial("List")
    </div>
</div>